severity:
  - CRITICAL
  - HIGH
  - MEDIUM

pkg:
  types:
    - os
    - library

ignorefile: .trivyignore

# Exclude virtual environments from secret scanning (false positives in third-party libs)
scan:
  skip-dirs:
    - .venv
    - venv
    - __pycache__
